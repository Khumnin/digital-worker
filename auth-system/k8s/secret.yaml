# TEMPLATE — do NOT commit real values.
# The eks-setup.sh script creates this Secret with actual values.
# To update a value manually:
#   kubectl create secret generic auth-api-secrets -n henderson \
#     --from-literal=DATABASE_URL='...' \
#     --from-literal=REDIS_URL='...' \
#     --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: auth-api-secrets
  namespace: henderson
  labels:
    app.kubernetes.io/name: auth-api
    app.kubernetes.io/part-of: tigersoft-auth
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  REDIS_URL: "REPLACE_ME"
  JWT_RSA_PRIVATE_KEY_PEM: "REPLACE_ME"
  RESEND_API_KEY: "REPLACE_ME"
  OAUTH_GOOGLE_CLIENT_SECRET: ""
  PLATFORM_ADMIN_EMAIL: "REPLACE_ME"
  PLATFORM_ADMIN_PASSWORD: "REPLACE_ME"
