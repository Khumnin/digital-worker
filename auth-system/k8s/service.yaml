apiVersion: v1
kind: Service
metadata:
  name: auth-system
  namespace: henderson
  labels:
    app.kubernetes.io/name: auth-api
    app.kubernetes.io/part-of: tigersoft-auth
spec:
  type: ClusterIP
  selector:
    app: auth-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
